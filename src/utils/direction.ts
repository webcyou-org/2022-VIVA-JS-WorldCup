interface DirectionProps {
    padType: string;
    type: string;
    code: string;
    state: any;
}

export const changeDirection = ({
    padType,
    type,
    code,
    state,
}: DirectionProps) => {};
